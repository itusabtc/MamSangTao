'use client'
import { Search, Swords, UserPlus } from 'lucide-react'
import { useState } from 'react'
import { playLearningSound } from '@/lib/learning-audio'
type Person={user_id:string;display_name:string;username:string;avatar:string;xp:number;streak:number}
export function ProfileCommunity(){const [query,setQuery]=useState(''),[people,setPeople]=useState<Person[]>([]),[message,setMessage]=useState('');async function find(){const response=await fetch(`/api/community?q=${encodeURIComponent(query)}`);const data=await response.json() as {people?:Person[]};setPeople(data.people||[]);playLearningSound('tap')}async function act(action:'follow'|'challenge',targetId:string){const response=await fetch('/api/community',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({action,targetId})});if(response.ok){playLearningSound(action==='follow'?'reward':'correct');setMessage(action==='follow'?'Đã theo dõi bạn mới!':'Đã gửi lời thách đấu cờ vua!')}}return <section className="rounded-[28px] border border-hairline bg-white p-5 shadow-sm"><h2 className="text-2xl">Tìm và kết nối bạn bè</h2><p className="mt-1 text-sm text-ink/60">Tìm theo tên hoặc tên người dùng, sau đó theo dõi hay gửi lời thách đấu.</p><div className="mt-4 flex gap-2"><input value={query} onChange={e=>setQuery(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')void find()}} className="field" placeholder="Nhập tên bạn bè..." aria-label="Tìm bạn bè"/><button onClick={()=>void find()} className="btn btn-primary"><Search className="h-5 w-5"/>Tìm</button></div>{message?<p role="status" className="mt-3 rounded-xl bg-green-soft px-3 py-2 text-sm font-bold text-green">{message}</p>:null}<div className="mt-4 grid gap-3">{people.map(person=><article key={person.user_id} className="flex flex-wrap items-center gap-3 rounded-2xl bg-cream/70 p-3"><span className="grid h-12 w-12 place-items-center rounded-2xl bg-white text-2xl">{person.avatar}</span><div className="min-w-0 flex-1"><strong>{person.display_name}</strong><p className="text-xs text-ink/55">@{person.username} · {person.xp} XP · 🔥 {person.streak}</p></div><button onClick={()=>void act('follow',person.user_id)} className="btn border border-hairline bg-white"><UserPlus className="h-4 w-4"/>Theo dõi</button><button onClick={()=>void act('challenge',person.user_id)} className="btn btn-primary"><Swords className="h-4 w-4"/>Thách đấu</button></article>)}{query&&people.length===0?<p className="text-sm text-ink/55">Chưa tìm thấy người dùng phù hợp.</p>:null}</div></section>}
